{
  "name": "kanji-inspector",
  "version": "0.1.5",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "download": "ts-node scripts/download.ts",
    "treat": "ts-node scripts/treat.ts",
    "build": "tsc",
    "minify": " esbuild dist/data/*.js --minify --outdir=dist/data/ --allow-overwrite",
    "patch": "npm version patch",
    "publish": "npm run build && npm run minify && npm publish --access public",
    "test": "jest"
  },
  "dependencies": {
    "unzipper": "^0.12.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.21",
    "@types/unzipper": "^0.10.11",
    "esbuild": "^0.25.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "keywords": [
    "count data set for TypeScript modules",
    "kanji dictionary",
    "kanji inspector",
    "kanji metadata",
    "kanji strokes count",
    "kanji strokes",
    "Kanji",
    "Strokes",
    "TypeScript",
    "Unicode Codepoint",
    "Unicode",
    "Unihan",
    "漢字",
    "漢字辞書"
  ]
}
